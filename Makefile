clean:
	rm -rf build*
	rm -rf bin include lib lib64
	rm -rf *.so *-info *.cpy *.cpp *.hpp
	rm -rf setup_hddm_*.py
	rm -rf HDDM
	rm -rf xrootd
	rm -rf hdf5
	rm -rf xerces-c
	rm -rf schemas
	rm -rf share
